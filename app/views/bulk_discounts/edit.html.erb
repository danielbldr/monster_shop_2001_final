<%= render partial: "shared/bulk_discount_form", locals: { path: "/bulk_discounts/#{@bulk_discount.id}",
                                      method: :patch,
                                      button_name: "Update This Discount",
                                      item: @bulk_discount } %>
